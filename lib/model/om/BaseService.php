<?php

/**
 * Base class that represents a row from the 'EMT_SERVICE' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * 05/10/12 16:10:38
 *
 * @package    lib.model.om
 */
abstract class BaseService extends BaseObject  implements Persistent {


  const PEER = 'ServicePeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        ServicePeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the guid field.
	 * Note: this column has a database default value of: 'sys_guid()'
	 * @var        string
	 */
	protected $guid;

	/**
	 * The value for the application_id field.
	 * @var        int
	 */
	protected $application_id;

	/**
	 * The value for the applies_to_type_id field.
	 * @var        int
	 */
	protected $applies_to_type_id;

	/**
	 * The value for the active field.
	 * Note: this column has a database default value of: false
	 * @var        boolean
	 */
	protected $active;

	/**
	 * The value for the status field.
	 * Note: this column has a database default value of: 0
	 * @var        int
	 */
	protected $status;

	/**
	 * The value for the created_at field.
	 * @var        string
	 */
	protected $created_at;

	/**
	 * The value for the updated_at field.
	 * @var        string
	 */
	protected $updated_at;

	/**
	 * The value for the detail_partial field.
	 * @var        string
	 */
	protected $detail_partial;

	/**
	 * The value for the image_id field.
	 * @var        int
	 */
	protected $image_id;

	/**
	 * @var        Application
	 */
	protected $aApplication;

	/**
	 * @var        PrivacyNodeType
	 */
	protected $aPrivacyNodeType;

	/**
	 * @var        MediaItem
	 */
	protected $aMediaItem;

	/**
	 * @var        array ServiceI18n[] Collection to store aggregation of ServiceI18n objects.
	 */
	protected $collServiceI18ns;

	/**
	 * @var        Criteria The criteria used to select the current contents of collServiceI18ns.
	 */
	private $lastServiceI18nCriteria = null;

	/**
	 * @var        array MarketingPackageItem[] Collection to store aggregation of MarketingPackageItem objects.
	 */
	protected $collMarketingPackageItems;

	/**
	 * @var        Criteria The criteria used to select the current contents of collMarketingPackageItems.
	 */
	private $lastMarketingPackageItemCriteria = null;

	/**
	 * @var        array JobPlus[] Collection to store aggregation of JobPlus objects.
	 */
	protected $collJobPluss;

	/**
	 * @var        Criteria The criteria used to select the current contents of collJobPluss.
	 */
	private $lastJobPlusCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

  /**
   * The value for the culture field.
   * @var string
   */
  protected $culture;

	/**
	 * Initializes internal state of BaseService object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->guid = 'sys_guid()';
		$this->active = false;
		$this->status = 0;
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [guid] column value.
	 * 
	 * @return     string
	 */
	public function getGuid()
	{
		return $this->guid;
	}

	/**
	 * Get the [application_id] column value.
	 * 
	 * @return     int
	 */
	public function getApplicationId()
	{
		return $this->application_id;
	}

	/**
	 * Get the [applies_to_type_id] column value.
	 * 
	 * @return     int
	 */
	public function getAppliesToTypeId()
	{
		return $this->applies_to_type_id;
	}

	/**
	 * Get the [active] column value.
	 * 
	 * @return     boolean
	 */
	public function getActive()
	{
		return $this->active;
	}

	/**
	 * Get the [status] column value.
	 * 
	 * @return     int
	 */
	public function getStatus()
	{
		return $this->status;
	}

	/**
	 * Get the [optionally formatted] temporal [created_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getCreatedAt($format = 'Y-M-D H:i:s')
	{
		if ($this->created_at === null) {
			return null;
		}



		try {
			$dt = new DateTime($this->created_at);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [updated_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getUpdatedAt($format = 'Y-M-D H:i:s')
	{
		if ($this->updated_at === null) {
			return null;
		}



		try {
			$dt = new DateTime($this->updated_at);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->updated_at, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [detail_partial] column value.
	 * 
	 * @return     string
	 */
	public function getDetailPartial()
	{
		return $this->detail_partial;
	}

	/**
	 * Get the [image_id] column value.
	 * 
	 * @return     int
	 */
	public function getImageId()
	{
		return $this->image_id;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     Service The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = ServicePeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [guid] column.
	 * 
	 * @param      string $v new value
	 * @return     Service The current object (for fluent API support)
	 */
	public function setGuid($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->guid !== $v || $v === 'sys_guid()') {
			$this->guid = $v;
			$this->modifiedColumns[] = ServicePeer::GUID;
		}

		return $this;
	} // setGuid()

	/**
	 * Set the value of [application_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Service The current object (for fluent API support)
	 */
	public function setApplicationId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->application_id !== $v) {
			$this->application_id = $v;
			$this->modifiedColumns[] = ServicePeer::APPLICATION_ID;
		}

		if ($this->aApplication !== null && $this->aApplication->getId() !== $v) {
			$this->aApplication = null;
		}

		return $this;
	} // setApplicationId()

	/**
	 * Set the value of [applies_to_type_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Service The current object (for fluent API support)
	 */
	public function setAppliesToTypeId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->applies_to_type_id !== $v) {
			$this->applies_to_type_id = $v;
			$this->modifiedColumns[] = ServicePeer::APPLIES_TO_TYPE_ID;
		}

		if ($this->aPrivacyNodeType !== null && $this->aPrivacyNodeType->getId() !== $v) {
			$this->aPrivacyNodeType = null;
		}

		return $this;
	} // setAppliesToTypeId()

	/**
	 * Set the value of [active] column.
	 * 
	 * @param      boolean $v new value
	 * @return     Service The current object (for fluent API support)
	 */
	public function setActive($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->active !== $v || $v === false) {
			$this->active = $v;
			$this->modifiedColumns[] = ServicePeer::ACTIVE;
		}

		return $this;
	} // setActive()

	/**
	 * Set the value of [status] column.
	 * 
	 * @param      int $v new value
	 * @return     Service The current object (for fluent API support)
	 */
	public function setStatus($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->status !== $v || $v === 0) {
			$this->status = $v;
			$this->modifiedColumns[] = ServicePeer::STATUS;
		}

		return $this;
	} // setStatus()

	/**
	 * Sets the value of [created_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Service The current object (for fluent API support)
	 */
	public function setCreatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->created_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->created_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = ServicePeer::CREATED_AT;
			}
		} // if either are not null

		return $this;
	} // setCreatedAt()

	/**
	 * Sets the value of [updated_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Service The current object (for fluent API support)
	 */
	public function setUpdatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->updated_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->updated_at !== null && $tmpDt = new DateTime($this->updated_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->updated_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = ServicePeer::UPDATED_AT;
			}
		} // if either are not null

		return $this;
	} // setUpdatedAt()

	/**
	 * Set the value of [detail_partial] column.
	 * 
	 * @param      string $v new value
	 * @return     Service The current object (for fluent API support)
	 */
	public function setDetailPartial($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->detail_partial !== $v) {
			$this->detail_partial = $v;
			$this->modifiedColumns[] = ServicePeer::DETAIL_PARTIAL;
		}

		return $this;
	} // setDetailPartial()

	/**
	 * Set the value of [image_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Service The current object (for fluent API support)
	 */
	public function setImageId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->image_id !== $v) {
			$this->image_id = $v;
			$this->modifiedColumns[] = ServicePeer::IMAGE_ID;
		}

		if ($this->aMediaItem !== null && $this->aMediaItem->getId() !== $v) {
			$this->aMediaItem = null;
		}

		return $this;
	} // setImageId()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array(ServicePeer::GUID,ServicePeer::ACTIVE,ServicePeer::STATUS))) {
				return false;
			}

			if ($this->guid !== 'sys_guid()') {
				return false;
			}

			if ($this->active !== false) {
				return false;
			}

			if ($this->status !== 0) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->guid = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->application_id = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
			$this->applies_to_type_id = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
			$this->active = ($row[$startcol + 4] !== null) ? (boolean) $row[$startcol + 4] : null;
			$this->status = ($row[$startcol + 5] !== null) ? (int) $row[$startcol + 5] : null;
			$this->created_at = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->updated_at = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->detail_partial = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
			$this->image_id = ($row[$startcol + 9] !== null) ? (int) $row[$startcol + 9] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 10; // 10 = ServicePeer::NUM_COLUMNS - ServicePeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Service object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aApplication !== null && $this->application_id !== $this->aApplication->getId()) {
			$this->aApplication = null;
		}
		if ($this->aPrivacyNodeType !== null && $this->applies_to_type_id !== $this->aPrivacyNodeType->getId()) {
			$this->aPrivacyNodeType = null;
		}
		if ($this->aMediaItem !== null && $this->image_id !== $this->aMediaItem->getId()) {
			$this->aMediaItem = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ServicePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = ServicePeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aApplication = null;
			$this->aPrivacyNodeType = null;
			$this->aMediaItem = null;
			$this->collServiceI18ns = null;
			$this->lastServiceI18nCriteria = null;

			$this->collMarketingPackageItems = null;
			$this->lastMarketingPackageItemCriteria = null;

			$this->collJobPluss = null;
			$this->lastJobPlusCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseService:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ServicePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			ServicePeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BaseService:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseService:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


    if ($this->isNew() && !$this->isColumnModified(ServicePeer::CREATED_AT))
    {
      $this->setCreatedAt(time());
    }

    if ($this->isModified() && !$this->isColumnModified(ServicePeer::UPDATED_AT))
    {
      $this->setUpdatedAt(time());
    }

		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ServicePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BaseService:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			ServicePeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aApplication !== null) {
				if ($this->aApplication->isModified() || ($this->aApplication->getCulture() && $this->aApplication->getCurrentApplicationI18n()->isModified()) || $this->aApplication->isNew()) {
					$affectedRows += $this->aApplication->save($con);
				}
				$this->setApplication($this->aApplication);
			}

			if ($this->aPrivacyNodeType !== null) {
				if ($this->aPrivacyNodeType->isModified() || ($this->aPrivacyNodeType->getCulture() && $this->aPrivacyNodeType->getCurrentPrivacyNodeTypeI18n()->isModified()) || $this->aPrivacyNodeType->isNew()) {
					$affectedRows += $this->aPrivacyNodeType->save($con);
				}
				$this->setPrivacyNodeType($this->aPrivacyNodeType);
			}

			if ($this->aMediaItem !== null) {
				if ($this->aMediaItem->isModified() || $this->aMediaItem->isNew()) {
					$affectedRows += $this->aMediaItem->save($con);
				}
				$this->setMediaItem($this->aMediaItem);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = ServicePeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = ServicePeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += ServicePeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collServiceI18ns !== null) {
				foreach ($this->collServiceI18ns as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collMarketingPackageItems !== null) {
				foreach ($this->collMarketingPackageItems as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collJobPluss !== null) {
				foreach ($this->collJobPluss as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aApplication !== null) {
				if (!$this->aApplication->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aApplication->getValidationFailures());
				}
			}

			if ($this->aPrivacyNodeType !== null) {
				if (!$this->aPrivacyNodeType->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aPrivacyNodeType->getValidationFailures());
				}
			}

			if ($this->aMediaItem !== null) {
				if (!$this->aMediaItem->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aMediaItem->getValidationFailures());
				}
			}


			if (($retval = ServicePeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collServiceI18ns !== null) {
					foreach ($this->collServiceI18ns as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collMarketingPackageItems !== null) {
					foreach ($this->collMarketingPackageItems as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collJobPluss !== null) {
					foreach ($this->collJobPluss as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = ServicePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getGuid();
				break;
			case 2:
				return $this->getApplicationId();
				break;
			case 3:
				return $this->getAppliesToTypeId();
				break;
			case 4:
				return $this->getActive();
				break;
			case 5:
				return $this->getStatus();
				break;
			case 6:
				return $this->getCreatedAt();
				break;
			case 7:
				return $this->getUpdatedAt();
				break;
			case 8:
				return $this->getDetailPartial();
				break;
			case 9:
				return $this->getImageId();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = ServicePeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getGuid(),
			$keys[2] => $this->getApplicationId(),
			$keys[3] => $this->getAppliesToTypeId(),
			$keys[4] => $this->getActive(),
			$keys[5] => $this->getStatus(),
			$keys[6] => $this->getCreatedAt(),
			$keys[7] => $this->getUpdatedAt(),
			$keys[8] => $this->getDetailPartial(),
			$keys[9] => $this->getImageId(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = ServicePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setGuid($value);
				break;
			case 2:
				$this->setApplicationId($value);
				break;
			case 3:
				$this->setAppliesToTypeId($value);
				break;
			case 4:
				$this->setActive($value);
				break;
			case 5:
				$this->setStatus($value);
				break;
			case 6:
				$this->setCreatedAt($value);
				break;
			case 7:
				$this->setUpdatedAt($value);
				break;
			case 8:
				$this->setDetailPartial($value);
				break;
			case 9:
				$this->setImageId($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = ServicePeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setGuid($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setApplicationId($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setAppliesToTypeId($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setActive($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setStatus($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setCreatedAt($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setUpdatedAt($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setDetailPartial($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setImageId($arr[$keys[9]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(ServicePeer::DATABASE_NAME);

		if ($this->isColumnModified(ServicePeer::ID)) $criteria->add(ServicePeer::ID, $this->id);
		if ($this->isColumnModified(ServicePeer::GUID)) $criteria->add(ServicePeer::GUID, $this->guid);
		if ($this->isColumnModified(ServicePeer::APPLICATION_ID)) $criteria->add(ServicePeer::APPLICATION_ID, $this->application_id);
		if ($this->isColumnModified(ServicePeer::APPLIES_TO_TYPE_ID)) $criteria->add(ServicePeer::APPLIES_TO_TYPE_ID, $this->applies_to_type_id);
		if ($this->isColumnModified(ServicePeer::ACTIVE)) $criteria->add(ServicePeer::ACTIVE, $this->active);
		if ($this->isColumnModified(ServicePeer::STATUS)) $criteria->add(ServicePeer::STATUS, $this->status);
		if ($this->isColumnModified(ServicePeer::CREATED_AT)) $criteria->add(ServicePeer::CREATED_AT, $this->created_at);
		if ($this->isColumnModified(ServicePeer::UPDATED_AT)) $criteria->add(ServicePeer::UPDATED_AT, $this->updated_at);
		if ($this->isColumnModified(ServicePeer::DETAIL_PARTIAL)) $criteria->add(ServicePeer::DETAIL_PARTIAL, $this->detail_partial);
		if ($this->isColumnModified(ServicePeer::IMAGE_ID)) $criteria->add(ServicePeer::IMAGE_ID, $this->image_id);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(ServicePeer::DATABASE_NAME);

		$criteria->add(ServicePeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Service (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setGuid($this->guid);

		$copyObj->setApplicationId($this->application_id);

		$copyObj->setAppliesToTypeId($this->applies_to_type_id);

		$copyObj->setActive($this->active);

		$copyObj->setStatus($this->status);

		$copyObj->setCreatedAt($this->created_at);

		$copyObj->setUpdatedAt($this->updated_at);

		$copyObj->setDetailPartial($this->detail_partial);

		$copyObj->setImageId($this->image_id);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getServiceI18ns() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addServiceI18n($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getMarketingPackageItems() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addMarketingPackageItem($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getJobPluss() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addJobPlus($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Service Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     ServicePeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new ServicePeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Application object.
	 *
	 * @param      Application $v
	 * @return     Service The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setApplication(Application $v = null)
	{
		if ($v === null) {
			$this->setApplicationId(NULL);
		} else {
			$this->setApplicationId($v->getId());
		}

		$this->aApplication = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Application object, it will not be re-added.
		if ($v !== null) {
			$v->addService($this);
		}

		return $this;
	}


	/**
	 * Get the associated Application object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Application The associated Application object.
	 * @throws     PropelException
	 */
	public function getApplication(PropelPDO $con = null)
	{
		if ($this->aApplication === null && ($this->application_id !== null)) {
			$c = new Criteria(ApplicationPeer::DATABASE_NAME);
			$c->add(ApplicationPeer::ID, $this->application_id);
			$this->aApplication = ApplicationPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aApplication->addServices($this);
			 */
		}
		return $this->aApplication;
	}

	/**
	 * Declares an association between this object and a PrivacyNodeType object.
	 *
	 * @param      PrivacyNodeType $v
	 * @return     Service The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setPrivacyNodeType(PrivacyNodeType $v = null)
	{
		if ($v === null) {
			$this->setAppliesToTypeId(NULL);
		} else {
			$this->setAppliesToTypeId($v->getId());
		}

		$this->aPrivacyNodeType = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the PrivacyNodeType object, it will not be re-added.
		if ($v !== null) {
			$v->addService($this);
		}

		return $this;
	}


	/**
	 * Get the associated PrivacyNodeType object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     PrivacyNodeType The associated PrivacyNodeType object.
	 * @throws     PropelException
	 */
	public function getPrivacyNodeType(PropelPDO $con = null)
	{
		if ($this->aPrivacyNodeType === null && ($this->applies_to_type_id !== null)) {
			$c = new Criteria(PrivacyNodeTypePeer::DATABASE_NAME);
			$c->add(PrivacyNodeTypePeer::ID, $this->applies_to_type_id);
			$this->aPrivacyNodeType = PrivacyNodeTypePeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aPrivacyNodeType->addServices($this);
			 */
		}
		return $this->aPrivacyNodeType;
	}

	/**
	 * Declares an association between this object and a MediaItem object.
	 *
	 * @param      MediaItem $v
	 * @return     Service The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setMediaItem(MediaItem $v = null)
	{
		if ($v === null) {
			$this->setImageId(NULL);
		} else {
			$this->setImageId($v->getId());
		}

		$this->aMediaItem = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the MediaItem object, it will not be re-added.
		if ($v !== null) {
			$v->addService($this);
		}

		return $this;
	}


	/**
	 * Get the associated MediaItem object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     MediaItem The associated MediaItem object.
	 * @throws     PropelException
	 */
	public function getMediaItem(PropelPDO $con = null)
	{
		if ($this->aMediaItem === null && ($this->image_id !== null)) {
			$c = new Criteria(MediaItemPeer::DATABASE_NAME);
			$c->add(MediaItemPeer::ID, $this->image_id);
			$this->aMediaItem = MediaItemPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aMediaItem->addServices($this);
			 */
		}
		return $this->aMediaItem;
	}

	/**
	 * Clears out the collServiceI18ns collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addServiceI18ns()
	 */
	public function clearServiceI18ns()
	{
		$this->collServiceI18ns = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collServiceI18ns collection (array).
	 *
	 * By default this just sets the collServiceI18ns collection to an empty array (like clearcollServiceI18ns());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initServiceI18ns()
	{
		$this->collServiceI18ns = array();
	}

	/**
	 * Gets an array of ServiceI18n objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Service has previously been saved, it will retrieve
	 * related ServiceI18ns from storage. If this Service is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array ServiceI18n[]
	 * @throws     PropelException
	 */
	public function getServiceI18ns($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ServicePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collServiceI18ns === null) {
			if ($this->isNew()) {
			   $this->collServiceI18ns = array();
			} else {

				$criteria->add(ServiceI18nPeer::ID, $this->id);

				ServiceI18nPeer::addSelectColumns($criteria);
				$this->collServiceI18ns = ServiceI18nPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ServiceI18nPeer::ID, $this->id);

				ServiceI18nPeer::addSelectColumns($criteria);
				if (!isset($this->lastServiceI18nCriteria) || !$this->lastServiceI18nCriteria->equals($criteria)) {
					$this->collServiceI18ns = ServiceI18nPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastServiceI18nCriteria = $criteria;
		return $this->collServiceI18ns;
	}

	/**
	 * Returns the number of related ServiceI18n objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related ServiceI18n objects.
	 * @throws     PropelException
	 */
	public function countServiceI18ns(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ServicePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collServiceI18ns === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(ServiceI18nPeer::ID, $this->id);

				$count = ServiceI18nPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(ServiceI18nPeer::ID, $this->id);

				if (!isset($this->lastServiceI18nCriteria) || !$this->lastServiceI18nCriteria->equals($criteria)) {
					$count = ServiceI18nPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collServiceI18ns);
				}
			} else {
				$count = count($this->collServiceI18ns);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a ServiceI18n object to this object
	 * through the ServiceI18n foreign key attribute.
	 *
	 * @param      ServiceI18n $l ServiceI18n
	 * @return     void
	 * @throws     PropelException
	 */
	public function addServiceI18n(ServiceI18n $l)
	{
		if ($this->collServiceI18ns === null) {
			$this->initServiceI18ns();
		}
		if (!in_array($l, $this->collServiceI18ns, true)) { // only add it if the **same** object is not already associated
			array_push($this->collServiceI18ns, $l);
			$l->setService($this);
		}
	}

	/**
	 * Clears out the collMarketingPackageItems collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addMarketingPackageItems()
	 */
	public function clearMarketingPackageItems()
	{
		$this->collMarketingPackageItems = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collMarketingPackageItems collection (array).
	 *
	 * By default this just sets the collMarketingPackageItems collection to an empty array (like clearcollMarketingPackageItems());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initMarketingPackageItems()
	{
		$this->collMarketingPackageItems = array();
	}

	/**
	 * Gets an array of MarketingPackageItem objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Service has previously been saved, it will retrieve
	 * related MarketingPackageItems from storage. If this Service is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array MarketingPackageItem[]
	 * @throws     PropelException
	 */
	public function getMarketingPackageItems($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ServicePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMarketingPackageItems === null) {
			if ($this->isNew()) {
			   $this->collMarketingPackageItems = array();
			} else {

				$criteria->add(MarketingPackageItemPeer::SERVICE_ID, $this->id);

				MarketingPackageItemPeer::addSelectColumns($criteria);
				$this->collMarketingPackageItems = MarketingPackageItemPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(MarketingPackageItemPeer::SERVICE_ID, $this->id);

				MarketingPackageItemPeer::addSelectColumns($criteria);
				if (!isset($this->lastMarketingPackageItemCriteria) || !$this->lastMarketingPackageItemCriteria->equals($criteria)) {
					$this->collMarketingPackageItems = MarketingPackageItemPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastMarketingPackageItemCriteria = $criteria;
		return $this->collMarketingPackageItems;
	}

	/**
	 * Returns the number of related MarketingPackageItem objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related MarketingPackageItem objects.
	 * @throws     PropelException
	 */
	public function countMarketingPackageItems(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ServicePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collMarketingPackageItems === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(MarketingPackageItemPeer::SERVICE_ID, $this->id);

				$count = MarketingPackageItemPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(MarketingPackageItemPeer::SERVICE_ID, $this->id);

				if (!isset($this->lastMarketingPackageItemCriteria) || !$this->lastMarketingPackageItemCriteria->equals($criteria)) {
					$count = MarketingPackageItemPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collMarketingPackageItems);
				}
			} else {
				$count = count($this->collMarketingPackageItems);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a MarketingPackageItem object to this object
	 * through the MarketingPackageItem foreign key attribute.
	 *
	 * @param      MarketingPackageItem $l MarketingPackageItem
	 * @return     void
	 * @throws     PropelException
	 */
	public function addMarketingPackageItem(MarketingPackageItem $l)
	{
		if ($this->collMarketingPackageItems === null) {
			$this->initMarketingPackageItems();
		}
		if (!in_array($l, $this->collMarketingPackageItems, true)) { // only add it if the **same** object is not already associated
			array_push($this->collMarketingPackageItems, $l);
			$l->setService($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Service is new, it will return
	 * an empty collection; or if this Service has previously
	 * been saved, it will retrieve related MarketingPackageItems from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Service.
	 */
	public function getMarketingPackageItemsJoinMarketingPackage($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ServicePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMarketingPackageItems === null) {
			if ($this->isNew()) {
				$this->collMarketingPackageItems = array();
			} else {

				$criteria->add(MarketingPackageItemPeer::SERVICE_ID, $this->id);

				$this->collMarketingPackageItems = MarketingPackageItemPeer::doSelectJoinMarketingPackage($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MarketingPackageItemPeer::SERVICE_ID, $this->id);

			if (!isset($this->lastMarketingPackageItemCriteria) || !$this->lastMarketingPackageItemCriteria->equals($criteria)) {
				$this->collMarketingPackageItems = MarketingPackageItemPeer::doSelectJoinMarketingPackage($criteria, $con, $join_behavior);
			}
		}
		$this->lastMarketingPackageItemCriteria = $criteria;

		return $this->collMarketingPackageItems;
	}

	/**
	 * Clears out the collJobPluss collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addJobPluss()
	 */
	public function clearJobPluss()
	{
		$this->collJobPluss = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collJobPluss collection (array).
	 *
	 * By default this just sets the collJobPluss collection to an empty array (like clearcollJobPluss());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initJobPluss()
	{
		$this->collJobPluss = array();
	}

	/**
	 * Gets an array of JobPlus objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Service has previously been saved, it will retrieve
	 * related JobPluss from storage. If this Service is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array JobPlus[]
	 * @throws     PropelException
	 */
	public function getJobPluss($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ServicePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJobPluss === null) {
			if ($this->isNew()) {
			   $this->collJobPluss = array();
			} else {

				$criteria->add(JobPlusPeer::SERVICE_ID, $this->id);

				JobPlusPeer::addSelectColumns($criteria);
				$this->collJobPluss = JobPlusPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(JobPlusPeer::SERVICE_ID, $this->id);

				JobPlusPeer::addSelectColumns($criteria);
				if (!isset($this->lastJobPlusCriteria) || !$this->lastJobPlusCriteria->equals($criteria)) {
					$this->collJobPluss = JobPlusPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastJobPlusCriteria = $criteria;
		return $this->collJobPluss;
	}

	/**
	 * Returns the number of related JobPlus objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related JobPlus objects.
	 * @throws     PropelException
	 */
	public function countJobPluss(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ServicePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collJobPluss === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(JobPlusPeer::SERVICE_ID, $this->id);

				$count = JobPlusPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(JobPlusPeer::SERVICE_ID, $this->id);

				if (!isset($this->lastJobPlusCriteria) || !$this->lastJobPlusCriteria->equals($criteria)) {
					$count = JobPlusPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collJobPluss);
				}
			} else {
				$count = count($this->collJobPluss);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a JobPlus object to this object
	 * through the JobPlus foreign key attribute.
	 *
	 * @param      JobPlus $l JobPlus
	 * @return     void
	 * @throws     PropelException
	 */
	public function addJobPlus(JobPlus $l)
	{
		if ($this->collJobPluss === null) {
			$this->initJobPluss();
		}
		if (!in_array($l, $this->collJobPluss, true)) { // only add it if the **same** object is not already associated
			array_push($this->collJobPluss, $l);
			$l->setService($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Service is new, it will return
	 * an empty collection; or if this Service has previously
	 * been saved, it will retrieve related JobPluss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Service.
	 */
	public function getJobPlussJoinJob($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ServicePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJobPluss === null) {
			if ($this->isNew()) {
				$this->collJobPluss = array();
			} else {

				$criteria->add(JobPlusPeer::SERVICE_ID, $this->id);

				$this->collJobPluss = JobPlusPeer::doSelectJoinJob($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(JobPlusPeer::SERVICE_ID, $this->id);

			if (!isset($this->lastJobPlusCriteria) || !$this->lastJobPlusCriteria->equals($criteria)) {
				$this->collJobPluss = JobPlusPeer::doSelectJoinJob($criteria, $con, $join_behavior);
			}
		}
		$this->lastJobPlusCriteria = $criteria;

		return $this->collJobPluss;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Service is new, it will return
	 * an empty collection; or if this Service has previously
	 * been saved, it will retrieve related JobPluss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Service.
	 */
	public function getJobPlussJoinPurchaseItem($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ServicePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJobPluss === null) {
			if ($this->isNew()) {
				$this->collJobPluss = array();
			} else {

				$criteria->add(JobPlusPeer::SERVICE_ID, $this->id);

				$this->collJobPluss = JobPlusPeer::doSelectJoinPurchaseItem($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(JobPlusPeer::SERVICE_ID, $this->id);

			if (!isset($this->lastJobPlusCriteria) || !$this->lastJobPlusCriteria->equals($criteria)) {
				$this->collJobPluss = JobPlusPeer::doSelectJoinPurchaseItem($criteria, $con, $join_behavior);
			}
		}
		$this->lastJobPlusCriteria = $criteria;

		return $this->collJobPluss;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collServiceI18ns) {
				foreach ((array) $this->collServiceI18ns as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collMarketingPackageItems) {
				foreach ((array) $this->collMarketingPackageItems as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collJobPluss) {
				foreach ((array) $this->collJobPluss as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collServiceI18ns = null;
		$this->collMarketingPackageItems = null;
		$this->collJobPluss = null;
			$this->aApplication = null;
			$this->aPrivacyNodeType = null;
			$this->aMediaItem = null;
	}


  /**
   * Returns the culture.
   *
   * @return string The culture
   */
  public function getCulture()
  {
    return $this->culture;
  }

  /**
   * Sets the culture.
   *
   * @param string $culture The culture to set
   *
   * @return void
   */
  public function setCulture($culture)
  {
    $this->culture = $culture;
  }

  public function getName($culture = null)
  {
    return $this->getCurrentServiceI18n($culture)->getName();
  }

  public function setName($value, $culture = null)
  {
    $this->getCurrentServiceI18n($culture)->setName($value);
  }

  public function getIntroduction($culture = null)
  {
    return $this->getCurrentServiceI18n($culture)->getIntroduction();
  }

  public function setIntroduction($value, $culture = null)
  {
    $this->getCurrentServiceI18n($culture)->setIntroduction($value);
  }

  protected $current_i18n = array();

  public function getCurrentServiceI18n($culture = null)
  {
    if (is_null($culture))
    {
      $culture = is_null($this->culture) ? sfPropel::getDefaultCulture() : $this->culture;
    }

    if (!isset($this->current_i18n[$culture]))
    {
      $obj = ServiceI18nPeer::retrieveByPK($this->getId(), $culture);
      if ($obj)
      {
        $this->setServiceI18nForCulture($obj, $culture);
      }
      else
      {
        $this->setServiceI18nForCulture(new ServiceI18n(), $culture);
        $this->current_i18n[$culture]->setCulture($culture);
      }
    }

    return $this->current_i18n[$culture];
  }

  public function setServiceI18nForCulture($object, $culture)
  {
    $this->current_i18n[$culture] = $object;
    $this->addServiceI18n($object);
  }


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BaseService:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BaseService::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BaseService
