<?php

/**
 * Base class that represents a row from the 'EMT_MESSAGE' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * 05/10/12 16:10:13
 *
 * @package    lib.model.om
 */
abstract class BaseMessage extends BaseObject  implements Persistent {


  const PEER = 'MessagePeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        MessagePeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the thread_id field.
	 * @var        int
	 */
	protected $thread_id;

	/**
	 * The value for the sender_id field.
	 * @var        int
	 */
	protected $sender_id;

	/**
	 * The value for the sender_type_id field.
	 * @var        int
	 */
	protected $sender_type_id;

	/**
	 * The value for the subject field.
	 * @var        string
	 */
	protected $subject;

	/**
	 * The value for the body field.
	 * @var        string
	 */
	protected $body;

	/**
	 * The value for the folder_id field.
	 * Note: this column has a database default value of: 1
	 * @var        int
	 */
	protected $folder_id;

	/**
	 * The value for the message_type_id field.
	 * Note: this column has a database default value of: 1
	 * @var        int
	 */
	protected $message_type_id;

	/**
	 * The value for the deleted_at field.
	 * @var        string
	 */
	protected $deleted_at;

	/**
	 * The value for the created_at field.
	 * @var        string
	 */
	protected $created_at;

	/**
	 * The value for the related_object_id field.
	 * @var        int
	 */
	protected $related_object_id;

	/**
	 * The value for the related_object_type_id field.
	 * @var        int
	 */
	protected $related_object_type_id;

	/**
	 * @var        Message
	 */
	protected $aMessageRelatedByThreadId;

	/**
	 * @var        PrivacyNodeType
	 */
	protected $aPrivacyNodeTypeRelatedBySenderTypeId;

	/**
	 * @var        PrivacyNodeType
	 */
	protected $aPrivacyNodeTypeRelatedByRelatedObjectTypeId;

	/**
	 * @var        array Message[] Collection to store aggregation of Message objects.
	 */
	protected $collMessagesRelatedByThreadId;

	/**
	 * @var        Criteria The criteria used to select the current contents of collMessagesRelatedByThreadId.
	 */
	private $lastMessageRelatedByThreadIdCriteria = null;

	/**
	 * @var        array MessageRecipient[] Collection to store aggregation of MessageRecipient objects.
	 */
	protected $collMessageRecipients;

	/**
	 * @var        Criteria The criteria used to select the current contents of collMessageRecipients.
	 */
	private $lastMessageRecipientCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Initializes internal state of BaseMessage object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->folder_id = 1;
		$this->message_type_id = 1;
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [thread_id] column value.
	 * 
	 * @return     int
	 */
	public function getThreadId()
	{
		return $this->thread_id;
	}

	/**
	 * Get the [sender_id] column value.
	 * 
	 * @return     int
	 */
	public function getSenderId()
	{
		return $this->sender_id;
	}

	/**
	 * Get the [sender_type_id] column value.
	 * 
	 * @return     int
	 */
	public function getSenderTypeId()
	{
		return $this->sender_type_id;
	}

	/**
	 * Get the [subject] column value.
	 * 
	 * @return     string
	 */
	public function getSubject()
	{
		return $this->subject;
	}

	/**
	 * Get the [body] column value.
	 * 
	 * @return     string
	 */
	public function getBody()
	{
		return $this->body;
	}

	/**
	 * Get the [folder_id] column value.
	 * 
	 * @return     int
	 */
	public function getFolderId()
	{
		return $this->folder_id;
	}

	/**
	 * Get the [message_type_id] column value.
	 * 
	 * @return     int
	 */
	public function getMessageTypeId()
	{
		return $this->message_type_id;
	}

	/**
	 * Get the [optionally formatted] temporal [deleted_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDeletedAt($format = 'Y-M-D H:i:s')
	{
		if ($this->deleted_at === null) {
			return null;
		}



		try {
			$dt = new DateTime($this->deleted_at);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->deleted_at, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [created_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getCreatedAt($format = 'Y-M-D H:i:s')
	{
		if ($this->created_at === null) {
			return null;
		}



		try {
			$dt = new DateTime($this->created_at);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [related_object_id] column value.
	 * 
	 * @return     int
	 */
	public function getRelatedObjectId()
	{
		return $this->related_object_id;
	}

	/**
	 * Get the [related_object_type_id] column value.
	 * 
	 * @return     int
	 */
	public function getRelatedObjectTypeId()
	{
		return $this->related_object_type_id;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     Message The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = MessagePeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [thread_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Message The current object (for fluent API support)
	 */
	public function setThreadId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->thread_id !== $v) {
			$this->thread_id = $v;
			$this->modifiedColumns[] = MessagePeer::THREAD_ID;
		}

		if ($this->aMessageRelatedByThreadId !== null && $this->aMessageRelatedByThreadId->getId() !== $v) {
			$this->aMessageRelatedByThreadId = null;
		}

		return $this;
	} // setThreadId()

	/**
	 * Set the value of [sender_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Message The current object (for fluent API support)
	 */
	public function setSenderId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->sender_id !== $v) {
			$this->sender_id = $v;
			$this->modifiedColumns[] = MessagePeer::SENDER_ID;
		}

		return $this;
	} // setSenderId()

	/**
	 * Set the value of [sender_type_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Message The current object (for fluent API support)
	 */
	public function setSenderTypeId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->sender_type_id !== $v) {
			$this->sender_type_id = $v;
			$this->modifiedColumns[] = MessagePeer::SENDER_TYPE_ID;
		}

		if ($this->aPrivacyNodeTypeRelatedBySenderTypeId !== null && $this->aPrivacyNodeTypeRelatedBySenderTypeId->getId() !== $v) {
			$this->aPrivacyNodeTypeRelatedBySenderTypeId = null;
		}

		return $this;
	} // setSenderTypeId()

	/**
	 * Set the value of [subject] column.
	 * 
	 * @param      string $v new value
	 * @return     Message The current object (for fluent API support)
	 */
	public function setSubject($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->subject !== $v) {
			$this->subject = $v;
			$this->modifiedColumns[] = MessagePeer::SUBJECT;
		}

		return $this;
	} // setSubject()

	/**
	 * Set the value of [body] column.
	 * 
	 * @param      string $v new value
	 * @return     Message The current object (for fluent API support)
	 */
	public function setBody($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->body !== $v) {
			$this->body = $v;
			$this->modifiedColumns[] = MessagePeer::BODY;
		}

		return $this;
	} // setBody()

	/**
	 * Set the value of [folder_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Message The current object (for fluent API support)
	 */
	public function setFolderId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->folder_id !== $v || $v === 1) {
			$this->folder_id = $v;
			$this->modifiedColumns[] = MessagePeer::FOLDER_ID;
		}

		return $this;
	} // setFolderId()

	/**
	 * Set the value of [message_type_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Message The current object (for fluent API support)
	 */
	public function setMessageTypeId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->message_type_id !== $v || $v === 1) {
			$this->message_type_id = $v;
			$this->modifiedColumns[] = MessagePeer::MESSAGE_TYPE_ID;
		}

		return $this;
	} // setMessageTypeId()

	/**
	 * Sets the value of [deleted_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Message The current object (for fluent API support)
	 */
	public function setDeletedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->deleted_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->deleted_at !== null && $tmpDt = new DateTime($this->deleted_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->deleted_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = MessagePeer::DELETED_AT;
			}
		} // if either are not null

		return $this;
	} // setDeletedAt()

	/**
	 * Sets the value of [created_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Message The current object (for fluent API support)
	 */
	public function setCreatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->created_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->created_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = MessagePeer::CREATED_AT;
			}
		} // if either are not null

		return $this;
	} // setCreatedAt()

	/**
	 * Set the value of [related_object_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Message The current object (for fluent API support)
	 */
	public function setRelatedObjectId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->related_object_id !== $v) {
			$this->related_object_id = $v;
			$this->modifiedColumns[] = MessagePeer::RELATED_OBJECT_ID;
		}

		return $this;
	} // setRelatedObjectId()

	/**
	 * Set the value of [related_object_type_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Message The current object (for fluent API support)
	 */
	public function setRelatedObjectTypeId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->related_object_type_id !== $v) {
			$this->related_object_type_id = $v;
			$this->modifiedColumns[] = MessagePeer::RELATED_OBJECT_TYPE_ID;
		}

		if ($this->aPrivacyNodeTypeRelatedByRelatedObjectTypeId !== null && $this->aPrivacyNodeTypeRelatedByRelatedObjectTypeId->getId() !== $v) {
			$this->aPrivacyNodeTypeRelatedByRelatedObjectTypeId = null;
		}

		return $this;
	} // setRelatedObjectTypeId()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array(MessagePeer::FOLDER_ID,MessagePeer::MESSAGE_TYPE_ID))) {
				return false;
			}

			if ($this->folder_id !== 1) {
				return false;
			}

			if ($this->message_type_id !== 1) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->thread_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->sender_id = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
			$this->sender_type_id = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
			$this->subject = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->body = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->folder_id = ($row[$startcol + 6] !== null) ? (int) $row[$startcol + 6] : null;
			$this->message_type_id = ($row[$startcol + 7] !== null) ? (int) $row[$startcol + 7] : null;
			$this->deleted_at = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
			$this->created_at = ($row[$startcol + 9] !== null) ? (string) $row[$startcol + 9] : null;
			$this->related_object_id = ($row[$startcol + 10] !== null) ? (int) $row[$startcol + 10] : null;
			$this->related_object_type_id = ($row[$startcol + 11] !== null) ? (int) $row[$startcol + 11] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 12; // 12 = MessagePeer::NUM_COLUMNS - MessagePeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Message object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aMessageRelatedByThreadId !== null && $this->thread_id !== $this->aMessageRelatedByThreadId->getId()) {
			$this->aMessageRelatedByThreadId = null;
		}
		if ($this->aPrivacyNodeTypeRelatedBySenderTypeId !== null && $this->sender_type_id !== $this->aPrivacyNodeTypeRelatedBySenderTypeId->getId()) {
			$this->aPrivacyNodeTypeRelatedBySenderTypeId = null;
		}
		if ($this->aPrivacyNodeTypeRelatedByRelatedObjectTypeId !== null && $this->related_object_type_id !== $this->aPrivacyNodeTypeRelatedByRelatedObjectTypeId->getId()) {
			$this->aPrivacyNodeTypeRelatedByRelatedObjectTypeId = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(MessagePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = MessagePeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aMessageRelatedByThreadId = null;
			$this->aPrivacyNodeTypeRelatedBySenderTypeId = null;
			$this->aPrivacyNodeTypeRelatedByRelatedObjectTypeId = null;
			$this->collMessagesRelatedByThreadId = null;
			$this->lastMessageRelatedByThreadIdCriteria = null;

			$this->collMessageRecipients = null;
			$this->lastMessageRecipientCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseMessage:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(MessagePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			MessagePeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BaseMessage:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseMessage:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


    if ($this->isNew() && !$this->isColumnModified(MessagePeer::CREATED_AT))
    {
      $this->setCreatedAt(time());
    }

		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(MessagePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BaseMessage:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			MessagePeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aMessageRelatedByThreadId !== null) {
				if ($this->aMessageRelatedByThreadId->isModified() || $this->aMessageRelatedByThreadId->isNew()) {
					$affectedRows += $this->aMessageRelatedByThreadId->save($con);
				}
				$this->setMessageRelatedByThreadId($this->aMessageRelatedByThreadId);
			}

			if ($this->aPrivacyNodeTypeRelatedBySenderTypeId !== null) {
				if ($this->aPrivacyNodeTypeRelatedBySenderTypeId->isModified() || ($this->aPrivacyNodeTypeRelatedBySenderTypeId->getCulture() && $this->aPrivacyNodeTypeRelatedBySenderTypeId->getCurrentPrivacyNodeTypeI18n()->isModified()) || $this->aPrivacyNodeTypeRelatedBySenderTypeId->isNew()) {
					$affectedRows += $this->aPrivacyNodeTypeRelatedBySenderTypeId->save($con);
				}
				$this->setPrivacyNodeTypeRelatedBySenderTypeId($this->aPrivacyNodeTypeRelatedBySenderTypeId);
			}

			if ($this->aPrivacyNodeTypeRelatedByRelatedObjectTypeId !== null) {
				if ($this->aPrivacyNodeTypeRelatedByRelatedObjectTypeId->isModified() || ($this->aPrivacyNodeTypeRelatedByRelatedObjectTypeId->getCulture() && $this->aPrivacyNodeTypeRelatedByRelatedObjectTypeId->getCurrentPrivacyNodeTypeI18n()->isModified()) || $this->aPrivacyNodeTypeRelatedByRelatedObjectTypeId->isNew()) {
					$affectedRows += $this->aPrivacyNodeTypeRelatedByRelatedObjectTypeId->save($con);
				}
				$this->setPrivacyNodeTypeRelatedByRelatedObjectTypeId($this->aPrivacyNodeTypeRelatedByRelatedObjectTypeId);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = MessagePeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = MessagePeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += MessagePeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collMessagesRelatedByThreadId !== null) {
				foreach ($this->collMessagesRelatedByThreadId as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collMessageRecipients !== null) {
				foreach ($this->collMessageRecipients as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aMessageRelatedByThreadId !== null) {
				if (!$this->aMessageRelatedByThreadId->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aMessageRelatedByThreadId->getValidationFailures());
				}
			}

			if ($this->aPrivacyNodeTypeRelatedBySenderTypeId !== null) {
				if (!$this->aPrivacyNodeTypeRelatedBySenderTypeId->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aPrivacyNodeTypeRelatedBySenderTypeId->getValidationFailures());
				}
			}

			if ($this->aPrivacyNodeTypeRelatedByRelatedObjectTypeId !== null) {
				if (!$this->aPrivacyNodeTypeRelatedByRelatedObjectTypeId->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aPrivacyNodeTypeRelatedByRelatedObjectTypeId->getValidationFailures());
				}
			}


			if (($retval = MessagePeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collMessagesRelatedByThreadId !== null) {
					foreach ($this->collMessagesRelatedByThreadId as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collMessageRecipients !== null) {
					foreach ($this->collMessageRecipients as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = MessagePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getThreadId();
				break;
			case 2:
				return $this->getSenderId();
				break;
			case 3:
				return $this->getSenderTypeId();
				break;
			case 4:
				return $this->getSubject();
				break;
			case 5:
				return $this->getBody();
				break;
			case 6:
				return $this->getFolderId();
				break;
			case 7:
				return $this->getMessageTypeId();
				break;
			case 8:
				return $this->getDeletedAt();
				break;
			case 9:
				return $this->getCreatedAt();
				break;
			case 10:
				return $this->getRelatedObjectId();
				break;
			case 11:
				return $this->getRelatedObjectTypeId();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = MessagePeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getThreadId(),
			$keys[2] => $this->getSenderId(),
			$keys[3] => $this->getSenderTypeId(),
			$keys[4] => $this->getSubject(),
			$keys[5] => $this->getBody(),
			$keys[6] => $this->getFolderId(),
			$keys[7] => $this->getMessageTypeId(),
			$keys[8] => $this->getDeletedAt(),
			$keys[9] => $this->getCreatedAt(),
			$keys[10] => $this->getRelatedObjectId(),
			$keys[11] => $this->getRelatedObjectTypeId(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = MessagePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setThreadId($value);
				break;
			case 2:
				$this->setSenderId($value);
				break;
			case 3:
				$this->setSenderTypeId($value);
				break;
			case 4:
				$this->setSubject($value);
				break;
			case 5:
				$this->setBody($value);
				break;
			case 6:
				$this->setFolderId($value);
				break;
			case 7:
				$this->setMessageTypeId($value);
				break;
			case 8:
				$this->setDeletedAt($value);
				break;
			case 9:
				$this->setCreatedAt($value);
				break;
			case 10:
				$this->setRelatedObjectId($value);
				break;
			case 11:
				$this->setRelatedObjectTypeId($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = MessagePeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setThreadId($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setSenderId($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setSenderTypeId($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setSubject($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setBody($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setFolderId($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setMessageTypeId($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setDeletedAt($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setCreatedAt($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setRelatedObjectId($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setRelatedObjectTypeId($arr[$keys[11]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(MessagePeer::DATABASE_NAME);

		if ($this->isColumnModified(MessagePeer::ID)) $criteria->add(MessagePeer::ID, $this->id);
		if ($this->isColumnModified(MessagePeer::THREAD_ID)) $criteria->add(MessagePeer::THREAD_ID, $this->thread_id);
		if ($this->isColumnModified(MessagePeer::SENDER_ID)) $criteria->add(MessagePeer::SENDER_ID, $this->sender_id);
		if ($this->isColumnModified(MessagePeer::SENDER_TYPE_ID)) $criteria->add(MessagePeer::SENDER_TYPE_ID, $this->sender_type_id);
		if ($this->isColumnModified(MessagePeer::SUBJECT)) $criteria->add(MessagePeer::SUBJECT, $this->subject);
		if ($this->isColumnModified(MessagePeer::BODY)) $criteria->add(MessagePeer::BODY, $this->body);
		if ($this->isColumnModified(MessagePeer::FOLDER_ID)) $criteria->add(MessagePeer::FOLDER_ID, $this->folder_id);
		if ($this->isColumnModified(MessagePeer::MESSAGE_TYPE_ID)) $criteria->add(MessagePeer::MESSAGE_TYPE_ID, $this->message_type_id);
		if ($this->isColumnModified(MessagePeer::DELETED_AT)) $criteria->add(MessagePeer::DELETED_AT, $this->deleted_at);
		if ($this->isColumnModified(MessagePeer::CREATED_AT)) $criteria->add(MessagePeer::CREATED_AT, $this->created_at);
		if ($this->isColumnModified(MessagePeer::RELATED_OBJECT_ID)) $criteria->add(MessagePeer::RELATED_OBJECT_ID, $this->related_object_id);
		if ($this->isColumnModified(MessagePeer::RELATED_OBJECT_TYPE_ID)) $criteria->add(MessagePeer::RELATED_OBJECT_TYPE_ID, $this->related_object_type_id);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(MessagePeer::DATABASE_NAME);

		$criteria->add(MessagePeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Message (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setThreadId($this->thread_id);

		$copyObj->setSenderId($this->sender_id);

		$copyObj->setSenderTypeId($this->sender_type_id);

		$copyObj->setSubject($this->subject);

		$copyObj->setBody($this->body);

		$copyObj->setFolderId($this->folder_id);

		$copyObj->setMessageTypeId($this->message_type_id);

		$copyObj->setDeletedAt($this->deleted_at);

		$copyObj->setCreatedAt($this->created_at);

		$copyObj->setRelatedObjectId($this->related_object_id);

		$copyObj->setRelatedObjectTypeId($this->related_object_type_id);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getMessagesRelatedByThreadId() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addMessageRelatedByThreadId($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getMessageRecipients() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addMessageRecipient($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Message Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     MessagePeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new MessagePeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Message object.
	 *
	 * @param      Message $v
	 * @return     Message The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setMessageRelatedByThreadId(Message $v = null)
	{
		if ($v === null) {
			$this->setThreadId(NULL);
		} else {
			$this->setThreadId($v->getId());
		}

		$this->aMessageRelatedByThreadId = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Message object, it will not be re-added.
		if ($v !== null) {
			$v->addMessageRelatedByThreadId($this);
		}

		return $this;
	}


	/**
	 * Get the associated Message object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Message The associated Message object.
	 * @throws     PropelException
	 */
	public function getMessageRelatedByThreadId(PropelPDO $con = null)
	{
		if ($this->aMessageRelatedByThreadId === null && ($this->thread_id !== null)) {
			$c = new Criteria(MessagePeer::DATABASE_NAME);
			$c->add(MessagePeer::ID, $this->thread_id);
			$this->aMessageRelatedByThreadId = MessagePeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aMessageRelatedByThreadId->addMessagesRelatedByThreadId($this);
			 */
		}
		return $this->aMessageRelatedByThreadId;
	}

	/**
	 * Declares an association between this object and a PrivacyNodeType object.
	 *
	 * @param      PrivacyNodeType $v
	 * @return     Message The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setPrivacyNodeTypeRelatedBySenderTypeId(PrivacyNodeType $v = null)
	{
		if ($v === null) {
			$this->setSenderTypeId(NULL);
		} else {
			$this->setSenderTypeId($v->getId());
		}

		$this->aPrivacyNodeTypeRelatedBySenderTypeId = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the PrivacyNodeType object, it will not be re-added.
		if ($v !== null) {
			$v->addMessageRelatedBySenderTypeId($this);
		}

		return $this;
	}


	/**
	 * Get the associated PrivacyNodeType object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     PrivacyNodeType The associated PrivacyNodeType object.
	 * @throws     PropelException
	 */
	public function getPrivacyNodeTypeRelatedBySenderTypeId(PropelPDO $con = null)
	{
		if ($this->aPrivacyNodeTypeRelatedBySenderTypeId === null && ($this->sender_type_id !== null)) {
			$c = new Criteria(PrivacyNodeTypePeer::DATABASE_NAME);
			$c->add(PrivacyNodeTypePeer::ID, $this->sender_type_id);
			$this->aPrivacyNodeTypeRelatedBySenderTypeId = PrivacyNodeTypePeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aPrivacyNodeTypeRelatedBySenderTypeId->addMessagesRelatedBySenderTypeId($this);
			 */
		}
		return $this->aPrivacyNodeTypeRelatedBySenderTypeId;
	}

	/**
	 * Declares an association between this object and a PrivacyNodeType object.
	 *
	 * @param      PrivacyNodeType $v
	 * @return     Message The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setPrivacyNodeTypeRelatedByRelatedObjectTypeId(PrivacyNodeType $v = null)
	{
		if ($v === null) {
			$this->setRelatedObjectTypeId(NULL);
		} else {
			$this->setRelatedObjectTypeId($v->getId());
		}

		$this->aPrivacyNodeTypeRelatedByRelatedObjectTypeId = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the PrivacyNodeType object, it will not be re-added.
		if ($v !== null) {
			$v->addMessageRelatedByRelatedObjectTypeId($this);
		}

		return $this;
	}


	/**
	 * Get the associated PrivacyNodeType object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     PrivacyNodeType The associated PrivacyNodeType object.
	 * @throws     PropelException
	 */
	public function getPrivacyNodeTypeRelatedByRelatedObjectTypeId(PropelPDO $con = null)
	{
		if ($this->aPrivacyNodeTypeRelatedByRelatedObjectTypeId === null && ($this->related_object_type_id !== null)) {
			$c = new Criteria(PrivacyNodeTypePeer::DATABASE_NAME);
			$c->add(PrivacyNodeTypePeer::ID, $this->related_object_type_id);
			$this->aPrivacyNodeTypeRelatedByRelatedObjectTypeId = PrivacyNodeTypePeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aPrivacyNodeTypeRelatedByRelatedObjectTypeId->addMessagesRelatedByRelatedObjectTypeId($this);
			 */
		}
		return $this->aPrivacyNodeTypeRelatedByRelatedObjectTypeId;
	}

	/**
	 * Clears out the collMessagesRelatedByThreadId collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addMessagesRelatedByThreadId()
	 */
	public function clearMessagesRelatedByThreadId()
	{
		$this->collMessagesRelatedByThreadId = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collMessagesRelatedByThreadId collection (array).
	 *
	 * By default this just sets the collMessagesRelatedByThreadId collection to an empty array (like clearcollMessagesRelatedByThreadId());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initMessagesRelatedByThreadId()
	{
		$this->collMessagesRelatedByThreadId = array();
	}

	/**
	 * Gets an array of Message objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Message has previously been saved, it will retrieve
	 * related MessagesRelatedByThreadId from storage. If this Message is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Message[]
	 * @throws     PropelException
	 */
	public function getMessagesRelatedByThreadId($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MessagePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMessagesRelatedByThreadId === null) {
			if ($this->isNew()) {
			   $this->collMessagesRelatedByThreadId = array();
			} else {

				$criteria->add(MessagePeer::THREAD_ID, $this->id);

				MessagePeer::addSelectColumns($criteria);
				$this->collMessagesRelatedByThreadId = MessagePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(MessagePeer::THREAD_ID, $this->id);

				MessagePeer::addSelectColumns($criteria);
				if (!isset($this->lastMessageRelatedByThreadIdCriteria) || !$this->lastMessageRelatedByThreadIdCriteria->equals($criteria)) {
					$this->collMessagesRelatedByThreadId = MessagePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastMessageRelatedByThreadIdCriteria = $criteria;
		return $this->collMessagesRelatedByThreadId;
	}

	/**
	 * Returns the number of related Message objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Message objects.
	 * @throws     PropelException
	 */
	public function countMessagesRelatedByThreadId(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MessagePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collMessagesRelatedByThreadId === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(MessagePeer::THREAD_ID, $this->id);

				$count = MessagePeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(MessagePeer::THREAD_ID, $this->id);

				if (!isset($this->lastMessageRelatedByThreadIdCriteria) || !$this->lastMessageRelatedByThreadIdCriteria->equals($criteria)) {
					$count = MessagePeer::doCount($criteria, $con);
				} else {
					$count = count($this->collMessagesRelatedByThreadId);
				}
			} else {
				$count = count($this->collMessagesRelatedByThreadId);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Message object to this object
	 * through the Message foreign key attribute.
	 *
	 * @param      Message $l Message
	 * @return     void
	 * @throws     PropelException
	 */
	public function addMessageRelatedByThreadId(Message $l)
	{
		if ($this->collMessagesRelatedByThreadId === null) {
			$this->initMessagesRelatedByThreadId();
		}
		if (!in_array($l, $this->collMessagesRelatedByThreadId, true)) { // only add it if the **same** object is not already associated
			array_push($this->collMessagesRelatedByThreadId, $l);
			$l->setMessageRelatedByThreadId($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Message is new, it will return
	 * an empty collection; or if this Message has previously
	 * been saved, it will retrieve related MessagesRelatedByThreadId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Message.
	 */
	public function getMessagesRelatedByThreadIdJoinPrivacyNodeTypeRelatedBySenderTypeId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MessagePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMessagesRelatedByThreadId === null) {
			if ($this->isNew()) {
				$this->collMessagesRelatedByThreadId = array();
			} else {

				$criteria->add(MessagePeer::THREAD_ID, $this->id);

				$this->collMessagesRelatedByThreadId = MessagePeer::doSelectJoinPrivacyNodeTypeRelatedBySenderTypeId($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MessagePeer::THREAD_ID, $this->id);

			if (!isset($this->lastMessageRelatedByThreadIdCriteria) || !$this->lastMessageRelatedByThreadIdCriteria->equals($criteria)) {
				$this->collMessagesRelatedByThreadId = MessagePeer::doSelectJoinPrivacyNodeTypeRelatedBySenderTypeId($criteria, $con, $join_behavior);
			}
		}
		$this->lastMessageRelatedByThreadIdCriteria = $criteria;

		return $this->collMessagesRelatedByThreadId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Message is new, it will return
	 * an empty collection; or if this Message has previously
	 * been saved, it will retrieve related MessagesRelatedByThreadId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Message.
	 */
	public function getMessagesRelatedByThreadIdJoinPrivacyNodeTypeRelatedByRelatedObjectTypeId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MessagePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMessagesRelatedByThreadId === null) {
			if ($this->isNew()) {
				$this->collMessagesRelatedByThreadId = array();
			} else {

				$criteria->add(MessagePeer::THREAD_ID, $this->id);

				$this->collMessagesRelatedByThreadId = MessagePeer::doSelectJoinPrivacyNodeTypeRelatedByRelatedObjectTypeId($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MessagePeer::THREAD_ID, $this->id);

			if (!isset($this->lastMessageRelatedByThreadIdCriteria) || !$this->lastMessageRelatedByThreadIdCriteria->equals($criteria)) {
				$this->collMessagesRelatedByThreadId = MessagePeer::doSelectJoinPrivacyNodeTypeRelatedByRelatedObjectTypeId($criteria, $con, $join_behavior);
			}
		}
		$this->lastMessageRelatedByThreadIdCriteria = $criteria;

		return $this->collMessagesRelatedByThreadId;
	}

	/**
	 * Clears out the collMessageRecipients collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addMessageRecipients()
	 */
	public function clearMessageRecipients()
	{
		$this->collMessageRecipients = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collMessageRecipients collection (array).
	 *
	 * By default this just sets the collMessageRecipients collection to an empty array (like clearcollMessageRecipients());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initMessageRecipients()
	{
		$this->collMessageRecipients = array();
	}

	/**
	 * Gets an array of MessageRecipient objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Message has previously been saved, it will retrieve
	 * related MessageRecipients from storage. If this Message is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array MessageRecipient[]
	 * @throws     PropelException
	 */
	public function getMessageRecipients($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MessagePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMessageRecipients === null) {
			if ($this->isNew()) {
			   $this->collMessageRecipients = array();
			} else {

				$criteria->add(MessageRecipientPeer::MESSAGE_ID, $this->id);

				MessageRecipientPeer::addSelectColumns($criteria);
				$this->collMessageRecipients = MessageRecipientPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(MessageRecipientPeer::MESSAGE_ID, $this->id);

				MessageRecipientPeer::addSelectColumns($criteria);
				if (!isset($this->lastMessageRecipientCriteria) || !$this->lastMessageRecipientCriteria->equals($criteria)) {
					$this->collMessageRecipients = MessageRecipientPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastMessageRecipientCriteria = $criteria;
		return $this->collMessageRecipients;
	}

	/**
	 * Returns the number of related MessageRecipient objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related MessageRecipient objects.
	 * @throws     PropelException
	 */
	public function countMessageRecipients(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MessagePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collMessageRecipients === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(MessageRecipientPeer::MESSAGE_ID, $this->id);

				$count = MessageRecipientPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(MessageRecipientPeer::MESSAGE_ID, $this->id);

				if (!isset($this->lastMessageRecipientCriteria) || !$this->lastMessageRecipientCriteria->equals($criteria)) {
					$count = MessageRecipientPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collMessageRecipients);
				}
			} else {
				$count = count($this->collMessageRecipients);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a MessageRecipient object to this object
	 * through the MessageRecipient foreign key attribute.
	 *
	 * @param      MessageRecipient $l MessageRecipient
	 * @return     void
	 * @throws     PropelException
	 */
	public function addMessageRecipient(MessageRecipient $l)
	{
		if ($this->collMessageRecipients === null) {
			$this->initMessageRecipients();
		}
		if (!in_array($l, $this->collMessageRecipients, true)) { // only add it if the **same** object is not already associated
			array_push($this->collMessageRecipients, $l);
			$l->setMessage($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Message is new, it will return
	 * an empty collection; or if this Message has previously
	 * been saved, it will retrieve related MessageRecipients from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Message.
	 */
	public function getMessageRecipientsJoinPrivacyNodeType($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MessagePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMessageRecipients === null) {
			if ($this->isNew()) {
				$this->collMessageRecipients = array();
			} else {

				$criteria->add(MessageRecipientPeer::MESSAGE_ID, $this->id);

				$this->collMessageRecipients = MessageRecipientPeer::doSelectJoinPrivacyNodeType($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MessageRecipientPeer::MESSAGE_ID, $this->id);

			if (!isset($this->lastMessageRecipientCriteria) || !$this->lastMessageRecipientCriteria->equals($criteria)) {
				$this->collMessageRecipients = MessageRecipientPeer::doSelectJoinPrivacyNodeType($criteria, $con, $join_behavior);
			}
		}
		$this->lastMessageRecipientCriteria = $criteria;

		return $this->collMessageRecipients;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collMessagesRelatedByThreadId) {
				foreach ((array) $this->collMessagesRelatedByThreadId as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collMessageRecipients) {
				foreach ((array) $this->collMessageRecipients as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collMessagesRelatedByThreadId = null;
		$this->collMessageRecipients = null;
			$this->aMessageRelatedByThreadId = null;
			$this->aPrivacyNodeTypeRelatedBySenderTypeId = null;
			$this->aPrivacyNodeTypeRelatedByRelatedObjectTypeId = null;
	}


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BaseMessage:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BaseMessage::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BaseMessage
