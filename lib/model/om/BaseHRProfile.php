<?php

/**
 * Base class that represents a row from the 'EMT_HR_PROFILE' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * 03/07/12 22:24:32
 *
 * @package    lib.model.om
 */
abstract class BaseHRProfile extends BaseObject  implements Persistent {


  const PEER = 'HRProfilePeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        HRProfilePeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the owner_id field.
	 * @var        int
	 */
	protected $owner_id;

	/**
	 * The value for the owner_type_id field.
	 * @var        int
	 */
	protected $owner_type_id;

	/**
	 * The value for the name field.
	 * @var        string
	 */
	protected $name;

	/**
	 * The value for the logo_id field.
	 * @var        int
	 */
	protected $logo_id;

	/**
	 * The value for the default_template_id field.
	 * @var        int
	 */
	protected $default_template_id;

	/**
	 * The value for the template_data field.
	 * @var        string
	 */
	protected $template_data;

	/**
	 * @var        PrivacyNodeType
	 */
	protected $aPrivacyNodeType;

	/**
	 * @var        MediaItem
	 */
	protected $aMediaItem;

	/**
	 * @var        array ResumeFolder[] Collection to store aggregation of ResumeFolder objects.
	 */
	protected $collResumeFolders;

	/**
	 * @var        Criteria The criteria used to select the current contents of collResumeFolders.
	 */
	private $lastResumeFolderCriteria = null;

	/**
	 * @var        array DatabaseCV[] Collection to store aggregation of DatabaseCV objects.
	 */
	protected $collDatabaseCVs;

	/**
	 * @var        Criteria The criteria used to select the current contents of collDatabaseCVs.
	 */
	private $lastDatabaseCVCriteria = null;

	/**
	 * @var        array JobMessageTemplate[] Collection to store aggregation of JobMessageTemplate objects.
	 */
	protected $collJobMessageTemplates;

	/**
	 * @var        Criteria The criteria used to select the current contents of collJobMessageTemplates.
	 */
	private $lastJobMessageTemplateCriteria = null;

	/**
	 * @var        array JobMessage[] Collection to store aggregation of JobMessage objects.
	 */
	protected $collJobMessages;

	/**
	 * @var        Criteria The criteria used to select the current contents of collJobMessages.
	 */
	private $lastJobMessageCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Initializes internal state of BaseHRProfile object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [owner_id] column value.
	 * 
	 * @return     int
	 */
	public function getOwnerId()
	{
		return $this->owner_id;
	}

	/**
	 * Get the [owner_type_id] column value.
	 * 
	 * @return     int
	 */
	public function getOwnerTypeId()
	{
		return $this->owner_type_id;
	}

	/**
	 * Get the [name] column value.
	 * 
	 * @return     string
	 */
	public function getName()
	{
		return $this->name;
	}

	/**
	 * Get the [logo_id] column value.
	 * 
	 * @return     int
	 */
	public function getLogoId()
	{
		return $this->logo_id;
	}

	/**
	 * Get the [default_template_id] column value.
	 * 
	 * @return     int
	 */
	public function getDefaultTemplateId()
	{
		return $this->default_template_id;
	}

	/**
	 * Get the [template_data] column value.
	 * 
	 * @return     string
	 */
	public function getTemplateData()
	{
		return $this->template_data;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     HRProfile The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = HRProfilePeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [owner_id] column.
	 * 
	 * @param      int $v new value
	 * @return     HRProfile The current object (for fluent API support)
	 */
	public function setOwnerId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->owner_id !== $v) {
			$this->owner_id = $v;
			$this->modifiedColumns[] = HRProfilePeer::OWNER_ID;
		}

		return $this;
	} // setOwnerId()

	/**
	 * Set the value of [owner_type_id] column.
	 * 
	 * @param      int $v new value
	 * @return     HRProfile The current object (for fluent API support)
	 */
	public function setOwnerTypeId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->owner_type_id !== $v) {
			$this->owner_type_id = $v;
			$this->modifiedColumns[] = HRProfilePeer::OWNER_TYPE_ID;
		}

		if ($this->aPrivacyNodeType !== null && $this->aPrivacyNodeType->getId() !== $v) {
			$this->aPrivacyNodeType = null;
		}

		return $this;
	} // setOwnerTypeId()

	/**
	 * Set the value of [name] column.
	 * 
	 * @param      string $v new value
	 * @return     HRProfile The current object (for fluent API support)
	 */
	public function setName($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->name !== $v) {
			$this->name = $v;
			$this->modifiedColumns[] = HRProfilePeer::NAME;
		}

		return $this;
	} // setName()

	/**
	 * Set the value of [logo_id] column.
	 * 
	 * @param      int $v new value
	 * @return     HRProfile The current object (for fluent API support)
	 */
	public function setLogoId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->logo_id !== $v) {
			$this->logo_id = $v;
			$this->modifiedColumns[] = HRProfilePeer::LOGO_ID;
		}

		if ($this->aMediaItem !== null && $this->aMediaItem->getId() !== $v) {
			$this->aMediaItem = null;
		}

		return $this;
	} // setLogoId()

	/**
	 * Set the value of [default_template_id] column.
	 * 
	 * @param      int $v new value
	 * @return     HRProfile The current object (for fluent API support)
	 */
	public function setDefaultTemplateId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->default_template_id !== $v) {
			$this->default_template_id = $v;
			$this->modifiedColumns[] = HRProfilePeer::DEFAULT_TEMPLATE_ID;
		}

		return $this;
	} // setDefaultTemplateId()

	/**
	 * Set the value of [template_data] column.
	 * 
	 * @param      string $v new value
	 * @return     HRProfile The current object (for fluent API support)
	 */
	public function setTemplateData($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->template_data !== $v) {
			$this->template_data = $v;
			$this->modifiedColumns[] = HRProfilePeer::TEMPLATE_DATA;
		}

		return $this;
	} // setTemplateData()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array())) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->owner_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->owner_type_id = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
			$this->name = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->logo_id = ($row[$startcol + 4] !== null) ? (int) $row[$startcol + 4] : null;
			$this->default_template_id = ($row[$startcol + 5] !== null) ? (int) $row[$startcol + 5] : null;
			$this->template_data = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 7; // 7 = HRProfilePeer::NUM_COLUMNS - HRProfilePeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating HRProfile object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aPrivacyNodeType !== null && $this->owner_type_id !== $this->aPrivacyNodeType->getId()) {
			$this->aPrivacyNodeType = null;
		}
		if ($this->aMediaItem !== null && $this->logo_id !== $this->aMediaItem->getId()) {
			$this->aMediaItem = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(HRProfilePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = HRProfilePeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aPrivacyNodeType = null;
			$this->aMediaItem = null;
			$this->collResumeFolders = null;
			$this->lastResumeFolderCriteria = null;

			$this->collDatabaseCVs = null;
			$this->lastDatabaseCVCriteria = null;

			$this->collJobMessageTemplates = null;
			$this->lastJobMessageTemplateCriteria = null;

			$this->collJobMessages = null;
			$this->lastJobMessageCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseHRProfile:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(HRProfilePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			HRProfilePeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BaseHRProfile:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseHRProfile:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(HRProfilePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BaseHRProfile:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			HRProfilePeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aPrivacyNodeType !== null) {
				if ($this->aPrivacyNodeType->isModified() || ($this->aPrivacyNodeType->getCulture() && $this->aPrivacyNodeType->getCurrentPrivacyNodeTypeI18n()->isModified()) || $this->aPrivacyNodeType->isNew()) {
					$affectedRows += $this->aPrivacyNodeType->save($con);
				}
				$this->setPrivacyNodeType($this->aPrivacyNodeType);
			}

			if ($this->aMediaItem !== null) {
				if ($this->aMediaItem->isModified() || $this->aMediaItem->isNew()) {
					$affectedRows += $this->aMediaItem->save($con);
				}
				$this->setMediaItem($this->aMediaItem);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = HRProfilePeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = HRProfilePeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += HRProfilePeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collResumeFolders !== null) {
				foreach ($this->collResumeFolders as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collDatabaseCVs !== null) {
				foreach ($this->collDatabaseCVs as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collJobMessageTemplates !== null) {
				foreach ($this->collJobMessageTemplates as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collJobMessages !== null) {
				foreach ($this->collJobMessages as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aPrivacyNodeType !== null) {
				if (!$this->aPrivacyNodeType->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aPrivacyNodeType->getValidationFailures());
				}
			}

			if ($this->aMediaItem !== null) {
				if (!$this->aMediaItem->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aMediaItem->getValidationFailures());
				}
			}


			if (($retval = HRProfilePeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collResumeFolders !== null) {
					foreach ($this->collResumeFolders as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collDatabaseCVs !== null) {
					foreach ($this->collDatabaseCVs as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collJobMessageTemplates !== null) {
					foreach ($this->collJobMessageTemplates as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collJobMessages !== null) {
					foreach ($this->collJobMessages as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = HRProfilePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getOwnerId();
				break;
			case 2:
				return $this->getOwnerTypeId();
				break;
			case 3:
				return $this->getName();
				break;
			case 4:
				return $this->getLogoId();
				break;
			case 5:
				return $this->getDefaultTemplateId();
				break;
			case 6:
				return $this->getTemplateData();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = HRProfilePeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getOwnerId(),
			$keys[2] => $this->getOwnerTypeId(),
			$keys[3] => $this->getName(),
			$keys[4] => $this->getLogoId(),
			$keys[5] => $this->getDefaultTemplateId(),
			$keys[6] => $this->getTemplateData(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = HRProfilePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setOwnerId($value);
				break;
			case 2:
				$this->setOwnerTypeId($value);
				break;
			case 3:
				$this->setName($value);
				break;
			case 4:
				$this->setLogoId($value);
				break;
			case 5:
				$this->setDefaultTemplateId($value);
				break;
			case 6:
				$this->setTemplateData($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = HRProfilePeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setOwnerId($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setOwnerTypeId($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setName($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setLogoId($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setDefaultTemplateId($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setTemplateData($arr[$keys[6]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(HRProfilePeer::DATABASE_NAME);

		if ($this->isColumnModified(HRProfilePeer::ID)) $criteria->add(HRProfilePeer::ID, $this->id);
		if ($this->isColumnModified(HRProfilePeer::OWNER_ID)) $criteria->add(HRProfilePeer::OWNER_ID, $this->owner_id);
		if ($this->isColumnModified(HRProfilePeer::OWNER_TYPE_ID)) $criteria->add(HRProfilePeer::OWNER_TYPE_ID, $this->owner_type_id);
		if ($this->isColumnModified(HRProfilePeer::NAME)) $criteria->add(HRProfilePeer::NAME, $this->name);
		if ($this->isColumnModified(HRProfilePeer::LOGO_ID)) $criteria->add(HRProfilePeer::LOGO_ID, $this->logo_id);
		if ($this->isColumnModified(HRProfilePeer::DEFAULT_TEMPLATE_ID)) $criteria->add(HRProfilePeer::DEFAULT_TEMPLATE_ID, $this->default_template_id);
		if ($this->isColumnModified(HRProfilePeer::TEMPLATE_DATA)) $criteria->add(HRProfilePeer::TEMPLATE_DATA, $this->template_data);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(HRProfilePeer::DATABASE_NAME);

		$criteria->add(HRProfilePeer::ID, $this->id);
		$criteria->add(HRProfilePeer::OWNER_TYPE_ID, $this->owner_type_id);

		return $criteria;
	}

	/**
	 * Returns the composite primary key for this object.
	 * The array elements will be in same order as specified in XML.
	 * @return     array
	 */
	public function getPrimaryKey()
	{
		$pks = array();

		$pks[0] = $this->getId();

		$pks[1] = $this->getOwnerTypeId();

		return $pks;
	}

	/**
	 * Set the [composite] primary key.
	 *
	 * @param      array $keys The elements of the composite key (order must match the order in XML file).
	 * @return     void
	 */
	public function setPrimaryKey($keys)
	{

		$this->setId($keys[0]);

		$this->setOwnerTypeId($keys[1]);

	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of HRProfile (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setOwnerId($this->owner_id);

		$copyObj->setOwnerTypeId($this->owner_type_id);

		$copyObj->setName($this->name);

		$copyObj->setLogoId($this->logo_id);

		$copyObj->setDefaultTemplateId($this->default_template_id);

		$copyObj->setTemplateData($this->template_data);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getResumeFolders() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addResumeFolder($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getDatabaseCVs() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addDatabaseCV($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getJobMessageTemplates() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addJobMessageTemplate($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getJobMessages() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addJobMessage($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     HRProfile Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     HRProfilePeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new HRProfilePeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a PrivacyNodeType object.
	 *
	 * @param      PrivacyNodeType $v
	 * @return     HRProfile The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setPrivacyNodeType(PrivacyNodeType $v = null)
	{
		if ($v === null) {
			$this->setOwnerTypeId(NULL);
		} else {
			$this->setOwnerTypeId($v->getId());
		}

		$this->aPrivacyNodeType = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the PrivacyNodeType object, it will not be re-added.
		if ($v !== null) {
			$v->addHRProfile($this);
		}

		return $this;
	}


	/**
	 * Get the associated PrivacyNodeType object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     PrivacyNodeType The associated PrivacyNodeType object.
	 * @throws     PropelException
	 */
	public function getPrivacyNodeType(PropelPDO $con = null)
	{
		if ($this->aPrivacyNodeType === null && ($this->owner_type_id !== null)) {
			$c = new Criteria(PrivacyNodeTypePeer::DATABASE_NAME);
			$c->add(PrivacyNodeTypePeer::ID, $this->owner_type_id);
			$this->aPrivacyNodeType = PrivacyNodeTypePeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aPrivacyNodeType->addHRProfiles($this);
			 */
		}
		return $this->aPrivacyNodeType;
	}

	/**
	 * Declares an association between this object and a MediaItem object.
	 *
	 * @param      MediaItem $v
	 * @return     HRProfile The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setMediaItem(MediaItem $v = null)
	{
		if ($v === null) {
			$this->setLogoId(NULL);
		} else {
			$this->setLogoId($v->getId());
		}

		$this->aMediaItem = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the MediaItem object, it will not be re-added.
		if ($v !== null) {
			$v->addHRProfile($this);
		}

		return $this;
	}


	/**
	 * Get the associated MediaItem object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     MediaItem The associated MediaItem object.
	 * @throws     PropelException
	 */
	public function getMediaItem(PropelPDO $con = null)
	{
		if ($this->aMediaItem === null && ($this->logo_id !== null)) {
			$c = new Criteria(MediaItemPeer::DATABASE_NAME);
			$c->add(MediaItemPeer::ID, $this->logo_id);
			$this->aMediaItem = MediaItemPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aMediaItem->addHRProfiles($this);
			 */
		}
		return $this->aMediaItem;
	}

	/**
	 * Clears out the collResumeFolders collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addResumeFolders()
	 */
	public function clearResumeFolders()
	{
		$this->collResumeFolders = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collResumeFolders collection (array).
	 *
	 * By default this just sets the collResumeFolders collection to an empty array (like clearcollResumeFolders());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initResumeFolders()
	{
		$this->collResumeFolders = array();
	}

	/**
	 * Gets an array of ResumeFolder objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this HRProfile has previously been saved, it will retrieve
	 * related ResumeFolders from storage. If this HRProfile is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array ResumeFolder[]
	 * @throws     PropelException
	 */
	public function getResumeFolders($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(HRProfilePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collResumeFolders === null) {
			if ($this->isNew()) {
			   $this->collResumeFolders = array();
			} else {

				$criteria->add(ResumeFolderPeer::HR_PROFILE_ID, $this->id);

				ResumeFolderPeer::addSelectColumns($criteria);
				$this->collResumeFolders = ResumeFolderPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ResumeFolderPeer::HR_PROFILE_ID, $this->id);

				ResumeFolderPeer::addSelectColumns($criteria);
				if (!isset($this->lastResumeFolderCriteria) || !$this->lastResumeFolderCriteria->equals($criteria)) {
					$this->collResumeFolders = ResumeFolderPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastResumeFolderCriteria = $criteria;
		return $this->collResumeFolders;
	}

	/**
	 * Returns the number of related ResumeFolder objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related ResumeFolder objects.
	 * @throws     PropelException
	 */
	public function countResumeFolders(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(HRProfilePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collResumeFolders === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(ResumeFolderPeer::HR_PROFILE_ID, $this->id);

				$count = ResumeFolderPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(ResumeFolderPeer::HR_PROFILE_ID, $this->id);

				if (!isset($this->lastResumeFolderCriteria) || !$this->lastResumeFolderCriteria->equals($criteria)) {
					$count = ResumeFolderPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collResumeFolders);
				}
			} else {
				$count = count($this->collResumeFolders);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a ResumeFolder object to this object
	 * through the ResumeFolder foreign key attribute.
	 *
	 * @param      ResumeFolder $l ResumeFolder
	 * @return     void
	 * @throws     PropelException
	 */
	public function addResumeFolder(ResumeFolder $l)
	{
		if ($this->collResumeFolders === null) {
			$this->initResumeFolders();
		}
		if (!in_array($l, $this->collResumeFolders, true)) { // only add it if the **same** object is not already associated
			array_push($this->collResumeFolders, $l);
			$l->setHRProfile($this);
		}
	}

	/**
	 * Clears out the collDatabaseCVs collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addDatabaseCVs()
	 */
	public function clearDatabaseCVs()
	{
		$this->collDatabaseCVs = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collDatabaseCVs collection (array).
	 *
	 * By default this just sets the collDatabaseCVs collection to an empty array (like clearcollDatabaseCVs());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initDatabaseCVs()
	{
		$this->collDatabaseCVs = array();
	}

	/**
	 * Gets an array of DatabaseCV objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this HRProfile has previously been saved, it will retrieve
	 * related DatabaseCVs from storage. If this HRProfile is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array DatabaseCV[]
	 * @throws     PropelException
	 */
	public function getDatabaseCVs($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(HRProfilePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDatabaseCVs === null) {
			if ($this->isNew()) {
			   $this->collDatabaseCVs = array();
			} else {

				$criteria->add(DatabaseCVPeer::HR_PROFILE_ID, $this->id);

				DatabaseCVPeer::addSelectColumns($criteria);
				$this->collDatabaseCVs = DatabaseCVPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(DatabaseCVPeer::HR_PROFILE_ID, $this->id);

				DatabaseCVPeer::addSelectColumns($criteria);
				if (!isset($this->lastDatabaseCVCriteria) || !$this->lastDatabaseCVCriteria->equals($criteria)) {
					$this->collDatabaseCVs = DatabaseCVPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastDatabaseCVCriteria = $criteria;
		return $this->collDatabaseCVs;
	}

	/**
	 * Returns the number of related DatabaseCV objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related DatabaseCV objects.
	 * @throws     PropelException
	 */
	public function countDatabaseCVs(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(HRProfilePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collDatabaseCVs === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(DatabaseCVPeer::HR_PROFILE_ID, $this->id);

				$count = DatabaseCVPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(DatabaseCVPeer::HR_PROFILE_ID, $this->id);

				if (!isset($this->lastDatabaseCVCriteria) || !$this->lastDatabaseCVCriteria->equals($criteria)) {
					$count = DatabaseCVPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collDatabaseCVs);
				}
			} else {
				$count = count($this->collDatabaseCVs);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a DatabaseCV object to this object
	 * through the DatabaseCV foreign key attribute.
	 *
	 * @param      DatabaseCV $l DatabaseCV
	 * @return     void
	 * @throws     PropelException
	 */
	public function addDatabaseCV(DatabaseCV $l)
	{
		if ($this->collDatabaseCVs === null) {
			$this->initDatabaseCVs();
		}
		if (!in_array($l, $this->collDatabaseCVs, true)) { // only add it if the **same** object is not already associated
			array_push($this->collDatabaseCVs, $l);
			$l->setHRProfile($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this HRProfile is new, it will return
	 * an empty collection; or if this HRProfile has previously
	 * been saved, it will retrieve related DatabaseCVs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in HRProfile.
	 */
	public function getDatabaseCVsJoinResume($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(HRProfilePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDatabaseCVs === null) {
			if ($this->isNew()) {
				$this->collDatabaseCVs = array();
			} else {

				$criteria->add(DatabaseCVPeer::HR_PROFILE_ID, $this->id);

				$this->collDatabaseCVs = DatabaseCVPeer::doSelectJoinResume($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DatabaseCVPeer::HR_PROFILE_ID, $this->id);

			if (!isset($this->lastDatabaseCVCriteria) || !$this->lastDatabaseCVCriteria->equals($criteria)) {
				$this->collDatabaseCVs = DatabaseCVPeer::doSelectJoinResume($criteria, $con, $join_behavior);
			}
		}
		$this->lastDatabaseCVCriteria = $criteria;

		return $this->collDatabaseCVs;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this HRProfile is new, it will return
	 * an empty collection; or if this HRProfile has previously
	 * been saved, it will retrieve related DatabaseCVs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in HRProfile.
	 */
	public function getDatabaseCVsJoinPurchaseItem($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(HRProfilePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDatabaseCVs === null) {
			if ($this->isNew()) {
				$this->collDatabaseCVs = array();
			} else {

				$criteria->add(DatabaseCVPeer::HR_PROFILE_ID, $this->id);

				$this->collDatabaseCVs = DatabaseCVPeer::doSelectJoinPurchaseItem($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DatabaseCVPeer::HR_PROFILE_ID, $this->id);

			if (!isset($this->lastDatabaseCVCriteria) || !$this->lastDatabaseCVCriteria->equals($criteria)) {
				$this->collDatabaseCVs = DatabaseCVPeer::doSelectJoinPurchaseItem($criteria, $con, $join_behavior);
			}
		}
		$this->lastDatabaseCVCriteria = $criteria;

		return $this->collDatabaseCVs;
	}

	/**
	 * Clears out the collJobMessageTemplates collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addJobMessageTemplates()
	 */
	public function clearJobMessageTemplates()
	{
		$this->collJobMessageTemplates = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collJobMessageTemplates collection (array).
	 *
	 * By default this just sets the collJobMessageTemplates collection to an empty array (like clearcollJobMessageTemplates());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initJobMessageTemplates()
	{
		$this->collJobMessageTemplates = array();
	}

	/**
	 * Gets an array of JobMessageTemplate objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this HRProfile has previously been saved, it will retrieve
	 * related JobMessageTemplates from storage. If this HRProfile is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array JobMessageTemplate[]
	 * @throws     PropelException
	 */
	public function getJobMessageTemplates($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(HRProfilePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJobMessageTemplates === null) {
			if ($this->isNew()) {
			   $this->collJobMessageTemplates = array();
			} else {

				$criteria->add(JobMessageTemplatePeer::HR_PROFILE_ID, $this->id);

				JobMessageTemplatePeer::addSelectColumns($criteria);
				$this->collJobMessageTemplates = JobMessageTemplatePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(JobMessageTemplatePeer::HR_PROFILE_ID, $this->id);

				JobMessageTemplatePeer::addSelectColumns($criteria);
				if (!isset($this->lastJobMessageTemplateCriteria) || !$this->lastJobMessageTemplateCriteria->equals($criteria)) {
					$this->collJobMessageTemplates = JobMessageTemplatePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastJobMessageTemplateCriteria = $criteria;
		return $this->collJobMessageTemplates;
	}

	/**
	 * Returns the number of related JobMessageTemplate objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related JobMessageTemplate objects.
	 * @throws     PropelException
	 */
	public function countJobMessageTemplates(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(HRProfilePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collJobMessageTemplates === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(JobMessageTemplatePeer::HR_PROFILE_ID, $this->id);

				$count = JobMessageTemplatePeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(JobMessageTemplatePeer::HR_PROFILE_ID, $this->id);

				if (!isset($this->lastJobMessageTemplateCriteria) || !$this->lastJobMessageTemplateCriteria->equals($criteria)) {
					$count = JobMessageTemplatePeer::doCount($criteria, $con);
				} else {
					$count = count($this->collJobMessageTemplates);
				}
			} else {
				$count = count($this->collJobMessageTemplates);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a JobMessageTemplate object to this object
	 * through the JobMessageTemplate foreign key attribute.
	 *
	 * @param      JobMessageTemplate $l JobMessageTemplate
	 * @return     void
	 * @throws     PropelException
	 */
	public function addJobMessageTemplate(JobMessageTemplate $l)
	{
		if ($this->collJobMessageTemplates === null) {
			$this->initJobMessageTemplates();
		}
		if (!in_array($l, $this->collJobMessageTemplates, true)) { // only add it if the **same** object is not already associated
			array_push($this->collJobMessageTemplates, $l);
			$l->setHRProfile($this);
		}
	}

	/**
	 * Clears out the collJobMessages collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addJobMessages()
	 */
	public function clearJobMessages()
	{
		$this->collJobMessages = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collJobMessages collection (array).
	 *
	 * By default this just sets the collJobMessages collection to an empty array (like clearcollJobMessages());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initJobMessages()
	{
		$this->collJobMessages = array();
	}

	/**
	 * Gets an array of JobMessage objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this HRProfile has previously been saved, it will retrieve
	 * related JobMessages from storage. If this HRProfile is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array JobMessage[]
	 * @throws     PropelException
	 */
	public function getJobMessages($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(HRProfilePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJobMessages === null) {
			if ($this->isNew()) {
			   $this->collJobMessages = array();
			} else {

				$criteria->add(JobMessagePeer::HR_PROFILE_ID, $this->id);

				JobMessagePeer::addSelectColumns($criteria);
				$this->collJobMessages = JobMessagePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(JobMessagePeer::HR_PROFILE_ID, $this->id);

				JobMessagePeer::addSelectColumns($criteria);
				if (!isset($this->lastJobMessageCriteria) || !$this->lastJobMessageCriteria->equals($criteria)) {
					$this->collJobMessages = JobMessagePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastJobMessageCriteria = $criteria;
		return $this->collJobMessages;
	}

	/**
	 * Returns the number of related JobMessage objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related JobMessage objects.
	 * @throws     PropelException
	 */
	public function countJobMessages(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(HRProfilePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collJobMessages === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(JobMessagePeer::HR_PROFILE_ID, $this->id);

				$count = JobMessagePeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(JobMessagePeer::HR_PROFILE_ID, $this->id);

				if (!isset($this->lastJobMessageCriteria) || !$this->lastJobMessageCriteria->equals($criteria)) {
					$count = JobMessagePeer::doCount($criteria, $con);
				} else {
					$count = count($this->collJobMessages);
				}
			} else {
				$count = count($this->collJobMessages);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a JobMessage object to this object
	 * through the JobMessage foreign key attribute.
	 *
	 * @param      JobMessage $l JobMessage
	 * @return     void
	 * @throws     PropelException
	 */
	public function addJobMessage(JobMessage $l)
	{
		if ($this->collJobMessages === null) {
			$this->initJobMessages();
		}
		if (!in_array($l, $this->collJobMessages, true)) { // only add it if the **same** object is not already associated
			array_push($this->collJobMessages, $l);
			$l->setHRProfile($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this HRProfile is new, it will return
	 * an empty collection; or if this HRProfile has previously
	 * been saved, it will retrieve related JobMessages from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in HRProfile.
	 */
	public function getJobMessagesJoinUser($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(HRProfilePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJobMessages === null) {
			if ($this->isNew()) {
				$this->collJobMessages = array();
			} else {

				$criteria->add(JobMessagePeer::HR_PROFILE_ID, $this->id);

				$this->collJobMessages = JobMessagePeer::doSelectJoinUser($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(JobMessagePeer::HR_PROFILE_ID, $this->id);

			if (!isset($this->lastJobMessageCriteria) || !$this->lastJobMessageCriteria->equals($criteria)) {
				$this->collJobMessages = JobMessagePeer::doSelectJoinUser($criteria, $con, $join_behavior);
			}
		}
		$this->lastJobMessageCriteria = $criteria;

		return $this->collJobMessages;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this HRProfile is new, it will return
	 * an empty collection; or if this HRProfile has previously
	 * been saved, it will retrieve related JobMessages from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in HRProfile.
	 */
	public function getJobMessagesJoinJob($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(HRProfilePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJobMessages === null) {
			if ($this->isNew()) {
				$this->collJobMessages = array();
			} else {

				$criteria->add(JobMessagePeer::HR_PROFILE_ID, $this->id);

				$this->collJobMessages = JobMessagePeer::doSelectJoinJob($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(JobMessagePeer::HR_PROFILE_ID, $this->id);

			if (!isset($this->lastJobMessageCriteria) || !$this->lastJobMessageCriteria->equals($criteria)) {
				$this->collJobMessages = JobMessagePeer::doSelectJoinJob($criteria, $con, $join_behavior);
			}
		}
		$this->lastJobMessageCriteria = $criteria;

		return $this->collJobMessages;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this HRProfile is new, it will return
	 * an empty collection; or if this HRProfile has previously
	 * been saved, it will retrieve related JobMessages from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in HRProfile.
	 */
	public function getJobMessagesJoinJobMessageTemplate($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(HRProfilePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJobMessages === null) {
			if ($this->isNew()) {
				$this->collJobMessages = array();
			} else {

				$criteria->add(JobMessagePeer::HR_PROFILE_ID, $this->id);

				$this->collJobMessages = JobMessagePeer::doSelectJoinJobMessageTemplate($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(JobMessagePeer::HR_PROFILE_ID, $this->id);

			if (!isset($this->lastJobMessageCriteria) || !$this->lastJobMessageCriteria->equals($criteria)) {
				$this->collJobMessages = JobMessagePeer::doSelectJoinJobMessageTemplate($criteria, $con, $join_behavior);
			}
		}
		$this->lastJobMessageCriteria = $criteria;

		return $this->collJobMessages;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collResumeFolders) {
				foreach ((array) $this->collResumeFolders as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collDatabaseCVs) {
				foreach ((array) $this->collDatabaseCVs as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collJobMessageTemplates) {
				foreach ((array) $this->collJobMessageTemplates as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collJobMessages) {
				foreach ((array) $this->collJobMessages as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collResumeFolders = null;
		$this->collDatabaseCVs = null;
		$this->collJobMessageTemplates = null;
		$this->collJobMessages = null;
			$this->aPrivacyNodeType = null;
			$this->aMediaItem = null;
	}


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BaseHRProfile:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BaseHRProfile::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BaseHRProfile
